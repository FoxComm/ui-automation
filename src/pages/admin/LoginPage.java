package pages.admin;

import base.AdminBasePage;

public class LoginPage extends AdminBasePage {

    /**
     * All elements are placed in BasePage, because
     * auth process maybe triggered in @BeforeClass of each test class.
     *
     * Though, some login page specific step methods might be placed here later.
     */

}